{
  "name": "openclaw-electron",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "main": "main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "electron:dev": "electron .",
    "electron:build": "electron-packager . OpenClaw --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "dependencies": {
    "electron": "^30.0.0",
    "electron-packager": "^17.1.2"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  }
}